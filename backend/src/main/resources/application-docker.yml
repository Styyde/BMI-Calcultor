spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/bmicalculator  # ⚠️ bmicalculator, PAS bmi_user
    username: bmi_user
    password: mysecretpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true